# Настройка Telegram интеграции

## Возможности

Интеграция с Telegram предоставляет следующий функционал:

1. ✅ **Автоматическое создание сделок** - когда новый пользователь пишет боту, автоматически создается контакт и сделка
2. ✅ **Сбор данных клиента** - автоматически сохраняются имя, фамилия, username, Telegram ID, аватарка
3. ✅ **Двусторонняя переписка** - менеджеры могут отвечать клиентам прямо из CRM
4. ✅ **Поддержка файлов** - отправка и прием файлов, фото, документов
5. ✅ **Голосовые сообщения** - прием и воспроизведение голосовых сообщений
6. ✅ **Видео и аудио** - полная поддержка медиа-файлов
7. ✅ **Надежное хранение** - все файлы скачиваются и хранятся локально в `/public/uploads/telegram`

## Шаг 1: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например, "MyCompany Support Bot")
4. Введите username бота (должен заканчиваться на `bot`, например `mycompany_support_bot`)
5. Скопируйте полученный **токен бота** (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройка .env файла

Добавьте в `.env` файл:

```env
TELEGRAM_BOT_TOKEN="ваш-токен-бота"
TELEGRAM_WEBHOOK_URL="https://ваш-домен.com/api/telegram/webhook"
```

**Важно:**
- `TELEGRAM_BOT_TOKEN` - токен, полученный от BotFather
- `TELEGRAM_WEBHOOK_URL` - публичный URL вашего приложения + `/api/telegram/webhook`

## Шаг 3: Деплой приложения

Задеплойте приложение на сервер с публичным доменом (например, Vercel, Railway, VPS).

**Важно:** Telegram требует HTTPS для webhook. Локальный `localhost` не будет работать.

## Шаг 4: Настройка webhook

После деплоя выполните:

```bash
# Установка webhook
npm run telegram:setup

# Или используя tsx напрямую
npx tsx scripts/setup-telegram-webhook.ts

# Для удаления webhook
npm run telegram:remove
```

Убедитесь, что webhook успешно установлен. Вы должны увидеть:

```
✅ Webhook успешно установлен!
📊 Информация о webhook:
  URL: https://ваш-домен.com/api/telegram/webhook
  Pending updates: 0
  Last error: Нет ошибок
```

## Шаг 5: Тестирование

1. Найдите вашего бота в Telegram по username
2. Нажмите "Start" или отправьте любое сообщение
3. В CRM автоматически должны создаться:
   - Новый контакт с данными из Telegram
   - Новая сделка на этапе "Новые"
4. Откройте сделку и проверьте чат - ваше сообщение должно отобразиться
5. Попробуйте ответить из CRM - сообщение должно прийти клиенту в Telegram

## Тестирование файлов

1. Отправьте боту:
   - Фото
   - Голосовое сообщение
   - Видео
   - Документ (PDF, Word, Excel и т.д.)
2. Откройте сделку в CRM и проверьте, что все файлы отображаются корректно
3. Попробуйте отправить файл из CRM клиенту

## Локальная разработка (опционально)

Для локальной разработки можно использовать polling режим вместо webhook:

1. Закомментируйте настройку webhook
2. Бот автоматически запустится в polling режиме при `NODE_ENV=development`
3. Это позволит тестировать без публичного домена

**Примечание:** В production всегда используйте webhook, так как это более надежно и эффективно.

## Структура данных

### Контакт (Contact)
- `telegramId` - уникальный ID пользователя в Telegram
- `telegramUsername` - @username пользователя
- `name` - имя и фамилия из профиля
- `source` - автоматически устанавливается в "telegram"

### Сообщения (Message)
- `type` - тип сообщения: TEXT, PHOTO, VOICE, AUDIO, VIDEO, VIDEO_NOTE, DOCUMENT
- `direction` - направление: IN (входящее) или OUT (исходящее)
- `content` - текст сообщения или описание файла
- `attachments` - вложения с файлами

### Вложения (Attachment)
- `filename` - имя файла
- `url` - путь к файлу (например, `/uploads/telegram/1234567890_photo.jpg`)
- `mimeType` - MIME тип файла
- `size` - размер файла в байтах

## Хранение файлов

Все файлы из Telegram автоматически скачиваются и сохраняются в:

```
/public/uploads/telegram/
```

Структура имени файла: `{timestamp}_{original_filename}`

Например:
- `1706234567890_photo.jpg`
- `1706234567891_document.pdf`
- `1706234567892_voice.ogg`

**Важно для продакшена:**
- Настройте регулярное резервное копирование директории `/public/uploads`
- Рассмотрите использование облачного хранилища (S3, Cloudinary) для больших объемов
- Добавьте ограничения на размер файлов в настройках Telegram бота

## Мониторинг

Для проверки статуса webhook выполните:

```bash
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo
```

Или используйте BotFather команду `/mybots` → выберите бота → Bot Settings → View Webhook Info

## Troubleshooting

### Webhook не работает
- Проверьте, что URL публичный и использует HTTPS
- Проверьте логи сервера на наличие ошибок
- Используйте команду `getWebhookInfo` для диагностики

### Сообщения не приходят
- Убедитесь, что webhook установлен корректно
- Проверьте, что токен бота правильный
- Проверьте логи Next.js приложения

### Файлы не скачиваются
- Проверьте права доступа к директории `/public/uploads/telegram`
- Убедитесь, что есть достаточно места на диске
- Проверьте ограничения на размер файлов

### Не создаются сделки
- Проверьте, что в базе данных есть таблицы `contacts` и `deals`
- Запустите миграции: `npx prisma migrate dev`
- Проверьте логи на наличие ошибок Prisma

## Дополнительные настройки

### Ограничение размера файлов

В Telegram ботах есть ограничение на скачивание файлов:
- Максимальный размер файла: 20 MB

### Приветственное сообщение

Добавьте приветственное сообщение для новых пользователей в `src/services/telegram.ts`:

```typescript
// После создания контакта отправляем приветствие
await bot.api.sendMessage(
  telegramId,
  'Здравствуйте! Ваше сообщение получено. Менеджер свяжется с вами в ближайшее время.'
)
```

## Безопасность

1. **Храните токен в секрете** - никогда не коммитьте `.env` файл
2. **Используйте HTTPS** - Telegram требует защищенное соединение
3. **Валидация данных** - все входящие данные проверяются перед сохранением
4. **Ограничение доступа** - только авторизованные менеджеры могут видеть сообщения

## Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Grammy Framework](https://grammy.dev/)
- [BotFather](https://t.me/botfather)
